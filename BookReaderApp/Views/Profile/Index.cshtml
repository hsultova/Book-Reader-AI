@model BookReaderApp.Models.ViewModels.ProfileViewModel
@{
    ViewData["Title"] = Model.DisplayName + " — Profile";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center gap-3 mb-3">
                    @if (!string.IsNullOrEmpty(Model.ProfilePicturePath))
                    {
                        <img src="@Model.ProfilePicturePath" alt="@Model.DisplayName"
                             class="rounded-circle object-fit-cover" width="96" height="96" />
                    }
                    else
                    {
                        <div class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center"
                             style="width:96px;height:96px;font-size:2rem;">
                            @(string.IsNullOrWhiteSpace(Model.DisplayName) ? "?" : Model.DisplayName.Trim()[0].ToString().ToUpper())
                        </div>
                    }
                    <div>
                        <h1 class="h3 mb-0">@Model.DisplayName</h1>
                        <small class="text-muted">Member since @Model.CreatedAt.ToString("MMMM yyyy")</small>
                    </div>
                </div>

                @if (!string.IsNullOrWhiteSpace(Model.Bio))
                {
                    <h2 class="h6 text-muted">About me</h2>
                    <p style="white-space: pre-wrap;">@Model.Bio</p>
                }

                <dl class="row mb-0">
                    @if (!string.IsNullOrWhiteSpace(Model.FavoriteGenre))
                    {
                        <dt class="col-sm-4">Favorite genre</dt>
                        <dd class="col-sm-8">@Model.FavoriteGenre</dd>
                    }
                    @if (Model.ReadingGoal.HasValue)
                    {
                        <dt class="col-sm-4">Reading goal</dt>
                        <dd class="col-sm-8">@Model.ReadingGoal book(s) per year</dd>
                    }
                </dl>

                @if (Model.IsOwnProfile)
                {
                    <a asp-action="Settings" class="btn btn-outline-primary mt-3">Edit profile</a>
                }
                else
                {
                    <div class="mt-3">
                        @switch (Model.FriendState)
                        {
                            case BookReaderApp.Models.ViewModels.FriendState.None:
                                <form asp-controller="Friends" asp-action="Send" method="post" class="d-inline">
                                    <input type="hidden" name="id" value="@Model.UserId" />
                                    <button type="submit" class="btn btn-primary">Add friend</button>
                                </form>
                                break;
                            case BookReaderApp.Models.ViewModels.FriendState.OutgoingPending:
                                <button type="button" class="btn btn-outline-secondary" disabled>Request sent</button>
                                break;
                            case BookReaderApp.Models.ViewModels.FriendState.IncomingPending:
                                <form asp-controller="Friends" asp-action="Accept" method="post" class="d-inline">
                                    <input type="hidden" name="id" value="@Model.PendingRequestId" />
                                    <button type="submit" class="btn btn-success">Accept friend request</button>
                                </form>
                                <form asp-controller="Friends" asp-action="Reject" method="post" class="d-inline">
                                    <input type="hidden" name="id" value="@Model.PendingRequestId" />
                                    <button type="submit" class="btn btn-outline-danger">Reject</button>
                                </form>
                                break;
                            case BookReaderApp.Models.ViewModels.FriendState.Friends:
                                <button type="button" class="btn btn-outline-success" disabled>
                                    <i class="bi bi-check-lg"></i> Friends
                                </button>
                                <form asp-controller="Messages" asp-action="Open" method="post" class="d-inline">
                                    <input type="hidden" name="id" value="@Model.UserId" />
                                    <button type="submit" class="btn btn-primary">Message</button>
                                </form>
                                break;
                        }
                    </div>
                }
            </div>
        </div>
    </div>
</div>
